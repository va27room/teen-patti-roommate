{
  "name": "teen-patti-roommate",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm --prefix client install && npm --prefix client run build",
    "start": "npm --prefix server install && node server/src.js",
    "test": "npm --prefix server test"
  },
  "engines": {
    "node": ">=20"
  }
}
