// Shared presentation timing keeps the server deadline and client exit in agreement.
// Shuffling, ante collection, seats, and turn selection remain in src.js.
export { buildDealPlan, buildDealSchedule } from "../shared/dealing.js";
